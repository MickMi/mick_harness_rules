interface:
  display_name: "Harness E2E"
  short_description: "Advance one requirement through delivery gates"
  default_prompt: "Use $harness-e2e for one requirement ID and show the current gate before requesting progress."
policy:
  allow_implicit_invocation: false
