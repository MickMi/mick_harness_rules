interface:
  display_name: "Harness Plan"
  short_description: "Preview or establish a project plan archive"
  default_prompt: "Use $harness-plan to scan this project and preview a safe plan archive."
policy:
  allow_implicit_invocation: false
