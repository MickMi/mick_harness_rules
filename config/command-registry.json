{
  "schema_version": "1",
  "product_version": "0.21.0",
  "canonical_interface": "harness <command>",
  "context_baseline": {
    "captured_at": "2026-08-24",
    "codex_global_loader_bytes": 13646,
    "project_loader_bytes": 18994,
    "combined_loader_bytes": 32640,
    "codex_default_discovery_limit_bytes": 32768,
    "remaining_headroom_bytes": 128,
    "full_regression_tests": 142
  },
  "context_budget": {
    "core_bytes": 10240,
    "project_loader_bytes": 16384,
    "global_loader_bytes": 12288,
    "combined_loader_bytes": 28672,
    "capsule_bytes": 4096,
    "checker": "scripts/harness-context-budget.py"
  },
  "execution_modes": {
    "default": "auto",
    "principle": "use the lightest process that can safely prove the user's result",
    "modes": {
      "auto": {
        "purpose": "select quick, standard, or e2e from the confirmed user intent and actual risk",
        "user_command_required": false
      },
      "quick": {
        "purpose": "finish a clear, reversible, low-risk task with one sufficient verification path",
        "creates_plan": false,
        "starts_role_workflow": false,
        "shows_self_test": false,
        "shows_round_card": false,
        "interrupts_user_on": [
          "the requested outcome becomes ambiguous",
          "the scope materially expands beyond the confirmed intent",
          "a destructive, privileged, costly, or externally visible action is required"
        ]
      },
      "standard": {
        "purpose": "deliver ordinary multi-file work or work with a small number of product decisions",
        "initial_confirmation": "only when a material product choice is unresolved",
        "shows_progress_at": "a meaningful milestone, user decision, blocker, or delivery"
      },
      "e2e": {
        "purpose": "deliver one clear requirement from confirmed intent to a release candidate",
        "initial_confirmation": "summarize intent, scope, assumptions, acceptance, and path before autonomous execution",
        "stops_at": "release_candidate",
        "interrupts_user_on": [
          "a new product decision falls outside the confirmed intent",
          "evidence disproves a material assumption or requires a direction change",
          "a destructive, privileged, costly, or externally visible action is required"
        ]
      }
    },
    "escalation": {
      "quick_to_standard": "explain the unexpected complexity before expanding the visible process when user intent may change",
      "standard_to_e2e": "requires explicit user confirmation",
      "never_changes": [
        "read before edit",
        "dangerous action confirmation",
        "anti-wall stop",
        "evidence-backed completion"
      ]
    }
  },
  "host_adapters": {
    "codex": {
      "reserved_commands": [
        "/plan",
        "/goal"
      ],
      "invocation": "$<skill-name>",
      "managed_skill_target": "~/.codex/skills",
      "skills": {
        "plan": "harness-plan",
        "goal": "harness-goal",
        "brain": "harness-brain",
        "e2e": "harness-e2e"
      }
    },
    "claude-code": {
      "reserved_commands": [],
      "invocation": "host-supported command or skill adapter",
      "managed_skill_target": "~/.claude/skills",
      "skills": {
        "plan": "harness-plan",
        "goal": "harness-goal",
        "brain": "harness-brain",
        "e2e": "harness-e2e"
      }
    }
  },
  "exit_codes": {
    "0": "requested preview or operation completed",
    "2": "a recoverable conflict or unmet precondition stopped the operation",
    "64": "invalid or missing user input",
    "69": "a required local service, host capability, or remote endpoint is unavailable",
    "74": "a required file could not be read or written safely"
  },
  "commands": [
    {
      "id": "plan",
      "cli": "harness plan",
      "purpose": "scan project facts and create or update the active plan archive",
      "preview_default": true,
      "write_flag": "--apply",
      "required_inputs": [],
      "optional_inputs": [
        "--project <path>",
        "--title <user-facing title>"
      ],
      "reads": [
        "project manifest and Harness rules",
        "plan.md when present",
        "docs/PROJECT.md and docs/VERSIONS.md when present",
        "Git branch and working-tree metadata"
      ],
      "writes": [
        "plan.md"
      ],
      "stops_on": [
        "an unrelated active plan would be overwritten",
        "the project path cannot be resolved",
        "the preview cannot identify a safe plan destination"
      ],
      "never_does": [
        "replace the host's native /plan command",
        "edit project source code",
        "change Git history or publish a release"
      ]
    },
    {
      "id": "goal",
      "cli": "harness goal",
      "purpose": "inspect and maintain the stable long-term project goal",
      "preview_default": true,
      "write_flag": "--apply",
      "required_inputs": [
        "--set <human project goal> when applying a change"
      ],
      "optional_inputs": [
        "--project <path>"
      ],
      "reads": [
        "docs/PROJECT.md when present",
        "plan.md and docs/VERSIONS.md for conflict detection"
      ],
      "writes": [
        "docs/PROJECT.md"
      ],
      "stops_on": [
        "the proposed goal is a version objective, implementation plan, or technical task",
        "the project path cannot be resolved",
        "an existing human goal would be replaced without an explicit preview"
      ],
      "never_does": [
        "replace the host's native /goal command",
        "turn a current version plan into the long-term goal",
        "edit project source code or publish a release"
      ]
    },
    {
      "id": "brain",
      "cli": "harness brain",
      "purpose": "explain, configure, install, and synchronize the optional Brain memory service",
      "preview_default": true,
      "write_flag": "configure --apply or an explicit install/sync action",
      "required_inputs": [
        "configure --mode local|remote|disabled when changing mode"
      ],
      "optional_inputs": [
        "status",
        "configure --remote <private Git URL>",
        "install",
        "sync"
      ],
      "reads": [
        "Harness Brain configuration",
        "local Brain repository state",
        "configured remote identity without credentials"
      ],
      "writes": [
        "Harness Brain configuration",
        "local Brain skeleton only after explicit local or remote setup",
        "approved or project-scoped Brain records during explicit synchronization"
      ],
      "modes": [
        "local",
        "remote",
        "disabled"
      ],
      "stops_on": [
        "remote mode has no remote URL",
        "the configured repository identity conflicts with the actual Git remote",
        "synchronization would expose credentials or an unapproved global candidate"
      ],
      "never_does": [
        "create Brain during ordinary harness init",
        "report local-only mode as a synchronization failure",
        "put prompts, chat transcripts, secrets, or environment variables into Brain events"
      ]
    },
    {
      "id": "e2e",
      "cli": "harness e2e",
      "purpose": "confirm one clear requirement intent and advance it autonomously to a release candidate",
      "preview_default": true,
      "write_flag": "--run",
      "required_inputs": [
        "--requirement <requirement_id>"
      ],
      "optional_inputs": [
        "--project <path>"
      ],
      "reads": [
        "the selected requirement and active version",
        "product-review, implementation, QA, and release-readiness evidence",
        "current Git branch and working-tree metadata"
      ],
      "writes": [
        "requirement-scoped workflow events and artifact references",
        "release-candidate state only when every required gate is satisfied"
      ],
      "stops_on": [
        "requirement_id is missing or ambiguous",
        "the initial intent, scope, assumptions, or acceptance criteria are not confirmed",
        "new evidence requires a material direction or scope change outside the confirmed intent",
        "a destructive, privileged, or externally visible action needs user approval"
      ],
      "never_does": [
        "merge, push, tag, deploy, or publish automatically",
        "continue after a material divergence from confirmed user intent",
        "combine multiple requirements into one hidden execution"
      ]
    }
  ]
}
